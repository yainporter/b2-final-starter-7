<%= render partial: "shared/adminnav" %>


<div class="row">
  <div class="col-sm-12 admin column-header">
    <p class="mr-auto">Invoices</p>
  </div>

   <% @invoices.each do |i| %>
    <li><%= link_to "Invoice ##{i.id}", admin_invoice_path(i) %></li>
  <% end %>

</div>
 
